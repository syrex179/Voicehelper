# Optional integrations. The application still starts with the Python standard library.
SpeechRecognition>=3.10,<4
vosk>=0.3.45,<0.4
PyAudio>=0.2.14; platform_system == "Windows"
Pillow>=10,<12
pycaw>=20240210; platform_system == "Windows"
comtypes>=1.4,<2; platform_system == "Windows"
pywin32>=306; platform_system == "Windows"
pystray>=0.19,<1; platform_system == "Windows"
openai>=1.0,<2
